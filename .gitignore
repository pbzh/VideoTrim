# PyInstaller build artifacts (bundles ffmpeg, ~500 MB — build locally)
/build/
/dist/
*.spec~

# Editor / OS
.claude/
.DS_Store
__pycache__/
*.pyc

# Local tooling (engram code graph + its npm deps)
node_modules/
.engram/
package.json
package-lock.json
